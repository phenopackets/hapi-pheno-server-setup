#!/usr/bin/env bash
set -x
set -e
set -u
set -o pipefail
set -o noclobber
shopt -s nullglob

rm -rf target
env | sort
eval " exec java ${HAPISETUP_HAPI_JAVA_OPTIONS} -jar ${HAPISETUP_HAPI_JAR} ${HAPISETUP_HAPI_ARGS}"


